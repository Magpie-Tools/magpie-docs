# Magpie Docs (Docusaurus)

This documentation site is built with Docusaurus and lives in `website/docs`.

## Local development

```bash
cd website/docs
npm install
npm run start
```

## Production build

```bash
cd website/docs
npm run build
```

The generated static output is written to `website/docs/build`.
